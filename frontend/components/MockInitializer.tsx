'use client';

/**
 * Mock API handlers are disabled.
 * The real Node backend (port 3001) is always running.
 * This component is kept as a no-op for backward compatibility.
 */
export default function MockInitializer() {
  return null;
}
