[project]
name = "orbitalquery-analysis"
version = "0.1.0"
description = "Earth Observation Analysis Service for OrbitalQuery"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: tests that require network access (deselect with '-m not integration')",
]
asyncio_mode = "auto"
